server:
  port: 8887

eureka:
  instance:
    hostname: localhost
  client:
    fetchRegistry: false #fetch all information from client
    registerWithEureka: false # register client, whitout thmslf
    serviceUrl:
      defaultZone: http://localhost:8887/eureka